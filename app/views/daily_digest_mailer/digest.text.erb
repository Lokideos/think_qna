Digest of newly created questions.
"<%= @greeting %>", "<%= @user.name %>". Digest of new questions is below.
<% @questions.each do |question| %>
  <%= link_to question.title, question_url(question) %>
<% end %>
